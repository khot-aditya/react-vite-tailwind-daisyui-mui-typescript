import React from 'react'
import PrimaryButton from '../../atoms/PrimaryButton'

const Login = () => {
    return (
        <div>
            <PrimaryButton>
                primary button
            </PrimaryButton>
        </div>
    )
}

export default Login
